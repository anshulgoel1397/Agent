# Agent

A tiny project with:

- `add_numbers.py`: a command-line program that adds two numbers.
- `index.html`: a browser-based calculator for adding two numbers.

## Run the Python Program

```bash
python add_numbers.py
```

## Run the Web App

Open `index.html` in a browser, enter two numbers, and click **Add numbers**.
